{
  "name": "diff-checker-pro",
  "version": "1.0.0",
  "description": "Compare text, code, or JSON side-by-side with highlighted differences",
  "main": "api/process.js",
  "scripts": {
    "dev": "node dev-server.js",
    "vercel-build": "echo 'Build complete'"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
